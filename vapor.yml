id: 41919
name: brokengg
environments:
  production:
    memory: 1024
    cli-memory: 512
    runtime: 'php-8.2:al2'
    build:
      - 'composer install --no-dev'
      - 'php artisan optimize'
      - 'npm install && npm vite build'
      - 'php artisan migrate:fresh --seed --force'
